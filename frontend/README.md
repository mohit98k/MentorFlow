components/ – Reusable UI building blocks
pages/ – Each URL = one page
layouts/ – Repeated page skeletons
context/ – Global state (Auth, Theme, etc.)
router/ – Central routing config
hooks/ - custom hook logics (they are not ui)

